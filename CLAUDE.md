## 泛微二开标准流程

**在本工作空间，所有泛微 e-cology 二次开发需求都必须走标准流程：需求分析 → 方案设计 → 执行 → 测试验收。人工介入仅限需求分析阶段，确认后全程自动。**

| 步骤 | SKILL | 触发时机 |
|------|-------|----------|
| 第一步：需求分析 | `oa-analyze-requirement` | 用户描述业务场景、提到任何功能想法 |
| 第二步：方案设计 | `oa-generate-design` | 需求确认后，用户问"怎么做"、"出方案" |
| 第三步：开发执行 | `oa-execute-dev` | 方案设计完成后自动进入执行 |
| 第四步：测试验收 | `oa-test-verify` | 执行完成后自动进入测试验收 |

- 每个项目文档按规范放在 `{项目名}/` 目录下，编号 01-05
- 每个 SKILL 都自动关联流程文档，知道自己在第几步、上下游是谁
- **人工只参与第一步的沟通确认，第二步至第四步全程自动执行完成**

## 操作优先级（泛微/二开相关）

当涉及泛微 OA、二开功能的增删改查操作时（如模块、表单、应用、字段、布局、浏览框、查询列表等），严格按以下顺序执行：

1. **先查 SDK**：使用 `search_sdk.py` 按中文描述搜索匹配的 SDK 方法，调用对应方法操作。
   - 搜索：`python3 /home/wangg/workspace/.claude/sdk/python/search_sdk.py 关键词`
   - 全列：`python3 /home/wangg/workspace/.claude/sdk/python/search_sdk.py`
   - SDK 封装了正确的 SQL 逻辑和表结构，是最准确的来源
2. **再查数据库**：SDK 无法满足时，通过 MCP Server 的数据库工具（`mcp__mssql-ecology__*`）直接操作数据库
3. **最后查其它**：以上都找不到时，再考虑翻 Java 源码、查文档等其他方式

**禁止跳过 SDK 直接操作数据库或翻源码。**

## 注意事项

1. **核心类在 JAR 中**：遇到找不到的类，大概率在 `classbean` 里，不要试图重新实现
2. **不是 git 仓库**：当前目录未初始化 git，操作前确认是否需要初始化
3. **数据库操作**：使用 `weaver.conn.RecordSet`，不要直接用 JDBC
4. **AI 负责思考，SDK 负责执行**：调用 SDK 前必须完成规则索引文档中的分析内容，SDK 只管执行不做分析
5. **查数据库前先确认表结构**：遇到不熟悉的表，必须先 `SELECT TOP 1 * FROM 表名` 拿到列名再写 SQL。**禁止凭经验猜列名或表名**。列名猜错会导致 Invalid column name 等报错，浪费 token 还返工。


## 规则索引

做以下操作时，**先读对应规则文件再动手**：

| 场景 | 规则文件 | 内容 |
|------|---------|------|
| 建模引擎（创建表单/模块/应用/查询列表/布局/字段联动） | `.claude/rules/modeling-rules.md` + `references/modeling-sdk-reference.md` | 通用原则（依赖排序、完整性检查）+ 各工具思考点和参数格式 |

**强制规则**：调用任何建模引擎 SDK 工具之前，必须先读 `references/modeling-sdk-reference.md` 中对应工具的"思考点"章节，完成业务分析后再调用。禁止跳过思考点直接调用 SDK。
